http:
  middlewares:
    cors:
      headers:
        accessControlAllowHeaders: "X-Token"
        accessControlAllowMethods: "OPTIONS,GET,POST,PUT,PATCH,DELETE"
        accessControlAllowCredentials: "true"
        accessControlAllowOriginListRegex: 'https?://([^.]+\.)?achieve.localhost(/.*)?'
